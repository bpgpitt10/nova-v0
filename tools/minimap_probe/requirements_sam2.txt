# Step 6 / fairway field-lab only. Hugging Face SAM2 requires torchvision for
# image preprocessing. Keep torch/torchvision as a matched release pair so a
# half-installed or incompatible vision runtime cannot slip through preflight.
torch==2.14.0
torchvision==0.29.0
transformers>=5.8.1,<6
pillow>=10,<13
huggingface-hub>=0.34,<2
packaging>=24
